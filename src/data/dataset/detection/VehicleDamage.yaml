defaults:
  - COCO
  - ../transforms@dataset_transforms: totensor

dataset_name: VehicleDamage

min_area: False
collate_fn: src.utils.data.collate_fn

train:
  _target_: src.data.dataset.COCO.COCOMultibbox
  root: ${data.dataset_dir}/vehicle_damage_detection/images/train
  json: ${data.dataset_dir}/vehicle_damage_detection/annotations/instances_train.json
  min_area: ${..min_area}
val:
  _target_: src.data.dataset.COCO.COCOMultibbox
  root: ${data.dataset_dir}/vehicle_damage_detection/images/val
  json: ${data.dataset_dir}/vehicle_damage_detection/annotations/instances_val.json
  min_area: ${..min_area}
test:
  _target_: src.data.dataset.COCO.COCOMultibbox
  root: ${data.dataset_dir}/vehicle_damage_detection/images/test
  json: ${data.dataset_dir}/vehicle_damage_detection/annotations/instances_test.json
  min_area: ${..min_area}